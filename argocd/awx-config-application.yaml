apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  labels:
    app: argocd-applications
    managedBy: argocd
    managementCodeGroup: chrismlee42
    managementCodeProject: argocd-applications
  name: awx
  namespace: argocd
spec:
  destination:
    name: in-cluster
    namespace: awx
  project: default
  sources:
    - path: argocd
      ref: path
      repoURL: https://github.com/chrismlee42/awx-config.git
      targetRevision: HEAD
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - RespectIgnoreDifferences=true
  ignoreDifferences:
    - group: '*'
      kind: '*'
      jqPathExpressions:
        - |
          def parsePath(pathStr):
            (pathStr | split(".")[1:]) as $parts | $parts | map(
              if test("^\\w+\\[\\d+\\]$") then
                capture("(?<key>\\w+)\\[(?<index>\\d+)\\]") | [.key, (.index | tonumber)]
              else
                [.]
              end
            ) | add
          ;
          . as $root | .metadata?.annotations?["custom.argocd.argoproj.io/ignore-path"] as $path | $root | getpath(parsePath($path))
    - kind: Secret
      name: postgresql-app
      namespace: awx
      jsonPointers:
        - /data
    - kind: Secret
      name: postgresql-superuser
      namespace: awx
      jsonPointers:
        - /data
    - kind: Secret
      name: postgresql-user
      namespace: awx
      jsonPointers:
        - /data
