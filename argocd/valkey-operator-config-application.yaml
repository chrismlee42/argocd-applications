apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: valkey-operator
spec:
  destination:
    name: in-cluster
    namespace: valkey-operator-system
  project: default
  sources:
    - path: argocd
      ref: path
      repoURL: https://github.com/chrismlee42/valkey-operator-config.git
      targetRevision: HEAD
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - ServerSideApply=true
