apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  labels:
    app: argocd-applications
    managedBy: argocd
    managementCodeGroup: chrismlee42
    managementCodeProject: argocd-applications
  name: secrets-store-csi-driver
  namespace: argocd
spec:
  destination:
    name: in-cluster
    namespace: kube-system
  project: default
  sources:
    - chart: secrets-store-csi-driver
      helm:
        releaseName: secrets-store-csi-driver
        valuesObject: {}
      ref: helm
      repoURL: https://kubernetes-sigs.github.io/secrets-store-csi-driver/charts
      targetRevision: "v1.5.3"
    - path: argocd
      ref: path
      repoURL: https://github.com/chrismlee42/secrets-store-csi-driver-config.git
      targetRevision: HEAD
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - ServerSideApply=true
